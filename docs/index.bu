# Bildup Documentation

^C(midnight, snow, electric)

::[pad=8 bg=primary fg=secondary]
# ^accent[Bildup]^
### The Markdown Superset for Interactive Static Pages.

::[pad=4 col flex gap=2]
^S(flex, row, gap=2)
::button[label="View Specification" primary action=navigate target="#spec"]/
::button[label="Comparison" action=navigate target="#comparison"]/
^S
::
::

::#philosophy[pad=6]
## Philosophy
Bildup was born from a simple observation: **Markdown is for documents, but the web is for applications.** 

Most "static site" solutions today require a complex build chain (Node, Webpack, React, etc.) just to get a button and a flexbox layout. Bildup bridges this gap by adding just enough syntax to Markdown to allow for **layout control** and **interactivity** without sacrificing the readability of a plain text file.

- **Zero-Build by Design**: Use `bildup-browser.js` to render `.bu` files directly. No NPM needed.
- **Token Efficient**: Designed to be easily generated and parsed by both humans and LLMs.
- **Self-Contained**: Compiles to a single HTML file with zero external dependencies (except `marked` if rendering in-browser).
::

::#comparison[pad=6 bg=surface fg=secondary]
## Comparison

^S(flex, row, gap=3)
::[pad=3 bg=primary fg=secondary]
#### vs. Standard Markdown
Markdown is great for text, but terrible for layout. In Bildup, you don't have to abuse tables for multi-column layouts or inject raw HTML `<div>` tags. 
::
::[pad=3 bg=primary fg=secondary]
#### vs. Raw HTML
HTML is powerful but verbose. Bildup's `::[attrs]` syntax is a "shorthand" for common styling patterns (padding, background, flex) that would otherwise require multiple CSS classes or inline styles.
::
^S

::table[striped border]
Feature, Standard Markdown, Raw HTML, Bildup
Layout Control, None (Single Column), Full (but verbose), Flexible (concise)
Interactivity, None, Full (requires JS), Declarative (Calc Engine)
Styling, Browser Default, Explicit CSS, Scoped Palettes
Authoring, Very Fast, Slow, Fast
::
::

::#spec[pad=6]
## Language Specification

### 1. Color Regions
Define a three-slot palette (`primary`, `secondary`, `accent`) scoped to a region.
```markdown
^C(crimson, obsidian, amber)
^primary[This text is crimson]^
^C
```
^C(crimson, obsidian, amber)
::[pad=1]
> **Demo**: ^primary[This text is crimson]^, ^secondary[this is obsidian]^, and ^accent[this is amber]^.
::
^C

---

### 2. Style Regions
Wrap content in a layout container (flex or grid).
```markdown
^S(flex, row, gap=4)
::[pad=2 bg=surface] Left ::
::[pad=2 bg=surface] Right ::
^S
```
^S(flex, row, gap=4)
::[pad=2 bg=surface] **Left Column** ::
::[pad=2 bg=surface] **Right Column** ::
^S

---

### 3. Blocks
Styled containers with padding, background, and text colors.
```markdown
::[pad=4 bg=electric fg=midnight]
# High Contrast Block
::
```
::[pad=4 bg=electric fg=midnight]
### High Contrast Block
This block uses the `electric` accent as a background.
::

---

### 4. Tables
CSV-based tables. No more pipe syntax.
```markdown
::table[striped border]
Name, Role, Level
Alice, Engineer, Lead
Bob, Designer, Senior
::
```
::table[striped border]
Name, Role, Level
Alice, Engineer, Lead
Bob, Designer, Senior
::

---

### 5. Interactive Components & Logic
Inputs, buttons, and calculations.

```markdown
::input#val1[number value=10]/
::input#val2[number value=5]/
::button[label="Add" action=calc targets="result"]/
::output#result[label="Sum:" fn=addFn]/

::fn#addFn
  val1 + val2
::fn
```

::[pad=4 bg=surface]
^S(flex, row, gap=2)
::input#v1[number value=10]/
::input#v2[number value=5]/
::button[label="Add" primary action=calc targets="res"]/
::output#res[label="Sum:" fn=add]/
::fn#add
  v1 + v2
::fn
^S
::

---

### 6. Script Passthrough
Drop into raw JavaScript when needed.
```markdown
::script
console.log("Bildup is active!");
::script
```

---

### 7. Source Inheritance
Reuse components and themes.
```markdown
::source ./theme.bu inline
::source ./navbar.bu
```
::

::#markdown[pad=6 bg=surface fg=secondary]
## Markdown Guide
All standard Markdown works exactly as expected.

^S(flex, row, gap=4)
::[pad=2]
#### Text Styles
- **Bold** with `**`
- *Italic* with `*`
- ~~Strikethrough~~ with `~~`
- `Inline Code` with backticks
::

::[pad=2]
#### Lists & Quotes
1. Ordered lists
2. Are supported
> "Blockquotes look great in the dark theme."
::
^S

#### Code Blocks
```javascript
function hello() {
  console.log("Hello, Bildup!");
}
```
::

::[pad=6 bg=primary fg=secondary]
Built with ^accent[Bildup]^. [Back to top](#)
::

^C
